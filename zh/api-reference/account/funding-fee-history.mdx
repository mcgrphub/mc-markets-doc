---
title: "资金费历史"
sidebarTitle: "资金费历史"
description: "资金费历史相关 OpenAPI 接口。"
---

本组对应 Spec tag `资金费历史记录`（对外名称 **资金费历史**）。路径前缀为 `/openapi/v1/mc-account/`。

## 接口列表

| 方法 | 路径 | 权限 | 描述 |
| --- | --- | --- | --- |
| GET | /openapi/v1/mc-account/funding-fee-record/page/v2 | READ | 资金费历史记录分页查询接口V2 |

<Note>
  Playground 已关闭。请按 [认证](/zh/authentication) 使用 AK/SK 签名后调用。Base URL 见 [基础 URL](/zh/api-reference/base-url)。
</Note>

---

## 资金费历史记录分页查询接口V2

`GET /openapi/v1/mc-account/funding-fee-record/page/v2` — 权限 `READ`。

feeType固定为INTEREST_FEE/FUNDING_FEE，按positionId+dealTime分组汇总amount

### 查询 / 路径参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| size | integer | 否 |  |
| current | integer | 否 |  |
| orderBy | string | 否 | 支持多字段排序，用逗号分隔 |
| orderDirection | string | 否 | ASC升序/DESC降序，多字段时用逗号分隔 |
| userId | string | 否 | 用户ID |
| login | string | 否 | mt5登录账号 |
| dealTimeStart | string | 否 | 开始扣费时间 |
| dealTimeEnd | string | 否 | 结束扣费时间 |

### 响应 `data` 字段（摘要）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| records | array |  |
| total | integer |  |
| size | integer |  |
| current | integer |  |
| pages | integer |  |
| hasPrevious | boolean |  |
| hasNext | boolean |  |

```bash
curl -sS -G "https://gateway.mcconnects.com/openapi/v1/mc-account/funding-fee-record/page/v2" \
  -H "X-Access-Key: YOUR_ACCESS_KEY" \
  -H "X-Signature: YOUR_SIGNATURE" \
  -H "X-Timestamp: YOUR_TIMESTAMP" \
  -H "X-Nonce: YOUR_NONCE"
  --data-urlencode "size=1" \
  --data-urlencode "current=1"
```
