---
title: "MC OpenAPI 快速开始"
sidebarTitle: "快速开始"
description: "创建 MC Access Key / Secret Key，使用 HMAC-SHA256 签名（含 X-Nonce）完成第一次带签名的网关调用。"
---

本指南将带你完成第一次调用 MC OpenAPI 的完整流程：创建 API Key、使用 **HMAC-SHA256** 对请求签名，并通过网关调用汇率接口。

<Note>
  开始前需要一个 MC 平台账号。如果没有，请联系你的 MC 对接人。每次请求须携带新的 `X-Nonce`（**UUID v4**，或 ≥32 位十六进制）。
</Note>

<Steps>
  <Step title="创建 API Key">
    登录 MC 平台后，打开[用户中心](https://www.mcmarkets.com/zh-Hans/uc/user-info)，或点击右上角账号菜单中的 **用户中心**。在 **API Key** 区域点击 **创建 API**。如果页面提示需要身份认证，请先按提示完成。

    <Frame>
      ![从用户中心进入 API Key 区域并点击创建 API](/images/guides/mcp/create-api-entry.png)
    </Frame>

    输入 API 名称，并勾选需要的权限：

    - `READ` — 只读操作（余额、列表、查询）
    - `TRADE` — 交易操作（下单 / 撤单）
    - `WITHDRAW` — 提现与划转操作

    使用 MCP 执行闪兑、存取、下单、改单、平仓或撤单时，请勾选 `TRADE` 权限。

    按需配置 IP 白名单（最多 15 个 IPv4 地址，多个地址用英文逗号分隔）。

    <Frame>
      ![填写 API 名称、权限和 IP 白名单](/images/guides/mcp/create-api-form.png)
    </Frame>

    点击 **确认**。若当前业务线启用了二次验证策略，请按页面提示完成 GA 验证和邮箱验证码校验。创建成功后，立即复制并妥善保存 **Access Key** 和 **Secret Key** —— Secret Key 仅显示一次。

    <Warning>
      切勿将 Secret Key 提交到源代码库，也不要在客户端代码中暴露。
    </Warning>
  </Step>
  <Step title="对请求签名">
    每个鉴权请求至少需要四个请求头：

    | Header | 说明 |
    | --- | --- |
    | `X-Access-Key` | 你的 Access Key |
    | `X-Timestamp` | 当前 Unix **毫秒** 时间戳（需在服务器时间 ±10 分钟内） |
    | `X-Nonce` | 每次请求新生成；**UUID v4**，或 ≥32 位十六进制（`0-9a-fA-F`） |
    | `X-Signature` | 待签名字符串的 HMAC-SHA256 结果的 **Base64** 编码 |

    用 `\n` 拼接以下部分构造待签名字符串，末尾附加一个 `\n`：

    ```text
    {HTTP_METHOD}
    {REQUEST_URI}
    {QUERY_STRING}       # 无 query 时整行省略（不要空行）
    {SIGNED_HEADERS}     # x-access-key&x-nonce&x-timestamp，小写并按 key 排序
    {TIMESTAMP}
    {NONCE}
    [{BODY_SHA256_HEX}]  # POST/PUT/PATCH/DELETE 且 body 非空时追加
    ```

    完整规则与 Python / Java / Node.js 示例请参见 [认证](/zh/authentication)。
  </Step>
  <Step title="调用汇率接口">
    `GET /openapi/v1/mc-account/exchange-rate/query/CRYPTO/BTC/USDT` 仅需 `READ` 权限，适合用来验证签名实现。

    ```bash
    ACCESS_KEY='YOUR_ACCESS_KEY'
    SECRET_KEY='YOUR_SECRET_KEY'
    URI='/openapi/v1/mc-account/exchange-rate/query/CRYPTO/BTC/USDT'
    TIMESTAMP=$(python3 -c 'import time; print(int(time.time()*1000))')
    NONCE=$(python3 -c 'import uuid; print(uuid.uuid4())')
    
    SIGNATURE=$(ACCESS_KEY="$ACCESS_KEY" SECRET_KEY="$SECRET_KEY" URI="$URI" TIMESTAMP="$TIMESTAMP" NONCE="$NONCE" python3 << 'PY'
    import base64, hashlib, hmac, os
    method = "GET"
    uri = os.environ["URI"]
    access_key = os.environ["ACCESS_KEY"]
    secret_key = os.environ["SECRET_KEY"]
    timestamp = os.environ["TIMESTAMP"]
    nonce = os.environ["NONCE"]
    headers = "&".join(
        f"{k}={v}"
        for k, v in sorted(
            {
                "x-access-key": access_key,
                "x-nonce": nonce,
                "x-timestamp": timestamp,
            }.items()
        )
    )
    string_to_sign = f"{method}\n{uri}\n{headers}\n{timestamp}\n{nonce}\n"
    sig = base64.b64encode(
        hmac.new(secret_key.encode(), string_to_sign.encode(), hashlib.sha256).digest()
    ).decode()
    print(sig)
    PY
    )
    
    curl -s \
      -H "X-Access-Key: ${ACCESS_KEY}" \
      -H "X-Signature: ${SIGNATURE}" \
      -H "X-Timestamp: ${TIMESTAMP}" \
      -H "X-Nonce: ${NONCE}" \
      "https://gateway.mcconnects.com${URI}"
    ```

    成功响应：

    ```json
    {
      "status": 200,
      "code": "200",
      "success": true,
      "data": 98500.12
    }
    ```
  </Step>
  <Step title="继续探索">
    <CardGroup cols={2}>
      <Card title="认证" icon="key" href="/zh/authentication">
        签名完整规则、路径 A/B、错误码与多语言示例。
      </Card>

      <Card title="基础 URL" icon="server" href="/zh/api-reference/base-url">
        生产网关地址。
      </Card>

      <Card title="API 参考" icon="code" href="/zh/api-reference/overview">
        全部公开接口。
      </Card>

      <Card title="第一次请求" icon="rocket" href="/zh/guides/first-request">
        带签名的端到端请求示例。
      </Card>

      <Card title="MCP 集成" icon="bot" href="/zh/guides/mcp">
        在 Claude、Cursor、VS Code 或 Codex 中使用 MCP Tool。
      </Card>
    </CardGroup>
  </Step>
</Steps>
