---
title: "Deals"
sidebarTitle: "Deals"
description: "OpenAPI endpoints for Deals."
---

Paths in this group use the `/openapi/v1/mc-trade/` prefix.

## Endpoints

| Method | Path | Permission | Description |
| --- | --- | --- | --- |
| GET | /openapi/v1/mc-trade/trade/deals/history/v3 | READ | Query historical deals V3 |
| GET | /openapi/v1/mc-trade/trade/deals/history/recent | Public | Query recent deals by symbol |

<Note>
  The interactive Playground is disabled. Sign requests with AK/SK as described in [Authentication](/authentication). See [Base URL](/api-reference/base-url) for the gateway host.
</Note>

---

## Query historical deals V3

`GET /openapi/v1/mc-trade/trade/deals/history/v3` — permission `READ`.

Query historical deals by UID and account data type (SPOT/CONTRACT/SPOT_CONTRACT).

### Query / path parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| size | integer | No | Page size |
| current | integer | No | Current page number |
| orderBy | string | No | Sort fields, comma-separated |
| sortOrder | string | No | Sort direction: ASC / DESC |
| type | string | Yes | Account data type: SPOT — spot, CONTRACT — contract, SPOT_CONTRACT — spot and contract combined |
| from | integer | Yes | Start timestamp (Unix timestamp, seconds) |
| to | integer | Yes | End timestamp (Unix timestamp, seconds) |
| symbol | string | No | Trading symbol name |

### Response `data` fields

| Field | Type | Description |
| --- | --- | --- |
| records | array | Records on the current page |
| total | integer | Total number of records |
| size | integer | Page size |
| current | integer | Current page number |
| pages | integer | Total number of pages |
| hasPrevious | boolean | Whether a previous page exists |
| hasNext | boolean | Whether a next page exists |

`records[]` items:

<Note>
  `deal`, `order`, `positionID`, and `login` are JSON strings. Do not deserialize them as numbers, or 64-bit identifiers may lose precision.
</Note>

| Field | Type | Description |
| --- | --- | --- |
| deal | string | Deal number and primary key |
| login | string | Trading account login |
| dealer | integer | Dealer ID |
| order | string | Related order number |
| action | integer | `0` buy, `1` sell |
| entry | integer | `0` open, `1` close |
| reason | integer | Deal-change reason code |
| digits | integer | Price precision |
| digitsCurrency | integer | Currency precision |
| contractSize | number | Standard contract size per lot |
| time | integer | Deal time in Unix milliseconds |
| timeMsc | integer | Deal time in Unix milliseconds |
| symbol | string | Trading symbol |
| price | number | Execution price |
| volumeExt | integer | Deal volume in extended precision |
| quantity | number | User-readable quantity converted from `volumeExt` |
| profit | number | Deal profit and loss |
| storage | number | Storage fee |
| commission | number | Commission |
| commissionDisplay | string | Commission display value |
| commissionScale | integer | Commission display precision |
| profitDisplay | string | PnL display value |
| profitScale | integer | PnL display precision |
| fee | number | Trading fee |
| rateProfit | number | Profit conversion rate |
| rateMargin | number | Margin conversion rate |
| profitRaw | number | Raw profit before conversion |
| pricePosition | number | Average position price |
| volumeClosedExt | integer | Closed volume in extended precision |
| tickValue | number | Tick value |
| tickSize | number | Tick size |
| flags | integer | Deal flags |
| value | number | Deal value |
| gateway | string | Gateway name |
| priceGateway | number | Gateway execution price |
| modifyFlags | integer | Deal modification flags |
| marketBid | number | Market bid price |
| marketAsk | number | Market ask price |
| marketLast | number | Last market price |
| volume | integer | Standard-precision volume |
| volumeClosed | integer | Closed standard-precision volume |
| externalID | string | External-system ID |
| expertID | integer | Expert advisor ID |
| positionID | string | Related position ID |
| priceSL | number | Stop-loss price |
| priceTP | number | Take-profit price |
| on_chain_status | string | On-chain status |
| tx_hash | string | On-chain transaction hash |

```bash
curl -sS -G "https://gateway.mcconnects.com/openapi/v1/mc-trade/trade/deals/history/v3" \
  -H "X-Access-Key: YOUR_ACCESS_KEY" \
  -H "X-Signature: YOUR_SIGNATURE" \
  -H "X-Timestamp: YOUR_TIMESTAMP" \
  -H "X-Nonce: YOUR_NONCE" \
  --data-urlencode "size=1" \
  --data-urlencode "current=1" \
  --data-urlencode "type=CONTRACT" \
  --data-urlencode "from=1755792000" \
  --data-urlencode "to=1755878400"
```

---

## Query recent deals by symbol

`GET /openapi/v1/mc-trade/trade/deals/history/recent` — permission `Public`.

Query recent deals by `symbol`. The `limit` range is 1 to 100. If `symbol` does not exist, the endpoint returns an empty array.

### Query / path parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| symbol | string | Yes | Trading symbol |
| limit | integer | Yes | Number of items to return; range is 1 to 100 |

### Response `data` fields

`data` is an array of objects. Element fields follow.

`data[]` items:

| Field | Type | Description |
| --- | --- | --- |
| action | integer | 0-Buy, 1-Sell |
| price | number | Deal Price |
| volumeExt | integer | Extended Volume |
| timeMsc | integer | Deal Time (ms) |
| orderId | integer | Order Number |

```bash
curl -sS -G "https://gateway.mcconnects.com/openapi/v1/mc-trade/trade/deals/history/recent" \
  --data-urlencode "symbol=BTCUSDT" \
  --data-urlencode "limit=1"
```
