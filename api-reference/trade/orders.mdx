---
title: "Orders"
sidebarTitle: "Orders"
description: "OpenAPI endpoints for Orders."
---

Paths in this group use the `/openapi/v1/mc-trade/` prefix.

## Endpoints

| Method | Path | Permission | Description |
| --- | --- | --- | --- |
| GET | /openapi/v1/mc-trade/trade/orders/pending/v2 | READ | Query pending (unfilled) orders V2 |
| GET | /openapi/v1/mc-trade/trade/orders/history/v3 | READ | 查询历史订单列表V3 |

<Note>
  The interactive Playground is disabled. Sign requests with AK/SK as described in [Authentication](/authentication). See [Base URL](/api-reference/base-url) for the gateway host.
</Note>

---

## Query pending (unfilled) orders V2

`GET /openapi/v1/mc-trade/trade/orders/pending/v2` — permission `READ`.

Query pending (unfilled) orders for the current user. UID is taken from the `X-UID` request header.

### Query / path parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| symbol | string | No |  |
| login | string | Yes |  |

```bash
curl -sS -G "https://gateway.mcconnects.com/openapi/v1/mc-trade/trade/orders/pending/v2" \
  -H "X-Access-Key: YOUR_ACCESS_KEY" \
  -H "X-Signature: YOUR_SIGNATURE" \
  -H "X-Timestamp: YOUR_TIMESTAMP" \
  -H "X-Nonce: YOUR_NONCE"
  --data-urlencode "login=VALUE"
```

---

## 查询历史订单列表V3

`GET /openapi/v1/mc-trade/trade/orders/history/v3` — permission `READ`.

根据 uid 和账户数据类型(SPOT/CONTRACT/SPOT_CONTRACT)查询历史委托订单列表，包含现货和合约数据

### Query / path parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| size | integer | No |  |
| current | integer | No |  |
| orderBy | string | No |  |
| sortOrder | string | No |  |
| type | string | Yes | 账户数据类型：SPOT-现货，CONTRACT-合约，SPOT_CONTRACT-现货与合约合并 |
| from | integer | Yes | 开始时间戳（Unix时间戳，秒） |
| to | integer | Yes | 结束时间戳（Unix时间戳，秒） |
| symbol | string | No | 交易品种名 |

### Response `data` fields (summary)

| Field | Type | Description |
| --- | --- | --- |
| records | array |  |
| total | integer |  |
| size | integer |  |
| current | integer |  |
| pages | integer |  |
| hasPrevious | boolean |  |
| hasNext | boolean |  |

```bash
curl -sS -G "https://gateway.mcconnects.com/openapi/v1/mc-trade/trade/orders/history/v3" \
  -H "X-Access-Key: YOUR_ACCESS_KEY" \
  -H "X-Signature: YOUR_SIGNATURE" \
  -H "X-Timestamp: YOUR_TIMESTAMP" \
  -H "X-Nonce: YOUR_NONCE"
  --data-urlencode "size=1" \
  --data-urlencode "current=1" \
  --data-urlencode "type=VALUE" \
  --data-urlencode "from=1755792000"
```
