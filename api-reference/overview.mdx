---
title: "API Reference"
sidebarTitle: "Overview"
description: "MC OpenAPI public endpoints generated from the openapi-exposed specifications, grouped by gateway service."
---

This reference covers every public MC OpenAPI endpoint (gateway paths with `@OpenApiPermission`). Pages under **Account**, **Trade**, **Risk**, and **Aggregator** are generated from OpenAPI Spec files in `openapi/`.

All endpoints share the same gateway host, authentication scheme (AK/SK with HMAC-SHA256), and JSON response envelope. Request URIs in the Spec already include the service segment used for signing (for example `/openapi/v1/mc-account/...`).

<CardGroup cols={2}>
  <Card title="Authentication" icon="lock" href="/authentication">
    AK/SK credentials, HMAC-SHA256 signing, timestamps, nonce, and permissions.
  </Card>

  <Card title="Base URL" icon="server" href="/api-reference/base-url">
    Production gateway host and path prefix.
  </Card>

  <Card title="Versioning" icon="code-branch" href="/api-reference/versioning">
    URL-path versioning under `/openapi/v1` and deprecation policy.
  </Card>

  <Card title="Error codes" icon="triangle-exclamation" href="/api-reference/error-codes">
    Full `code` / `codeKey` catalog from `ExceptionTypeEnum`.
  </Card>
</CardGroup>

## Endpoint groups

| Group | Spec | Gateway path prefix |
| --- | --- | --- |
| Account | `openapi/mc-account.json` | `/openapi/v1/mc-account/` |
| Trade | `openapi/mc-trade.json` | `/openapi/v1/mc-trade/` |
| Risk | `openapi/mc-risk.json` | `/openapi/v1/mc-risk/` |
| Aggregator | `openapi/mc-aggregator.json` | `/openapi/v1/mc-aggregator/` |

Use the sidebar groups to open individual operations. The interactive playground is disabled because requests require custom AK/SK signing — follow [Authentication](/authentication) and [First request](/guides/first-request).
