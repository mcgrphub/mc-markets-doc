---
title: "API Reference"
sidebarTitle: "Overview"
description: "MC OpenAPI public endpoints grouped by gateway service: Account, Trade, Risk, and Aggregator."
---

This reference covers public MC OpenAPI endpoints. The sidebar groups **Account**, **Trade**, **Risk**, and **Aggregator**.

All endpoints share the same gateway host, authentication scheme (AK/SK with HMAC-SHA256), and JSON response envelope. Request paths already include the service segment used for signing (for example `/openapi/v1/mc-account/...`).

<CardGroup cols={2}>
  <Card title="Authentication" icon="lock" href="/authentication">
    AK/SK credentials, HMAC-SHA256 signing, timestamps, nonce, and permissions.
  </Card>

  <Card title="Base URL" icon="server" href="/api-reference/base-url">
    Production gateway host and path prefix.
  </Card>

  <Card title="Versioning" icon="code-branch" href="/api-reference/versioning">
    URL-path versioning under `/openapi/v1` and deprecation policy.
  </Card>

  <Card title="Error codes" icon="triangle-exclamation" href="/api-reference/error-codes">
    Full `code` / `codeKey` catalog.
  </Card>
</CardGroup>

## Endpoint groups

| Group | Gateway path prefix |
| --- | --- |
| Account | `/openapi/v1/mc-account/` |
| Trade | `/openapi/v1/mc-trade/` |
| Risk | `/openapi/v1/mc-risk/` |
| Aggregator | `/openapi/v1/mc-aggregator/` |

Use the sidebar to open each group. The interactive playground is disabled because requests require custom AK/SK signing — follow [Authentication](/authentication) and [First request](/guides/first-request).
